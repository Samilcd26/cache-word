import 'package:flutter/material.dart';

class PreparedWordPage extends StatelessWidget {
  const PreparedWordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("saasdsad"),
    );
  }
}
